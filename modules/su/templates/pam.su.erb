<% if architecture == 'x86_64' %>
<% archlib='lib64' %>
<% else %>
<% archlib='lib' %>
<% end %>

#%PAM-1.0
auth       sufficient   /<%= archlib -%>/security/$ISA/pam_rootok.so
auth       required     /<%= archlib -%>/security/$ISA/pam_wheel.so use_uid
auth       required     /<%= archlib -%>/security/$ISA/pam_stack.so service=system-auth
account    required     /<%= archlib -%>/security/$ISA/pam_stack.so service=system-auth
password   required     /<%= archlib -%>/security/$ISA/pam_stack.so service=system-auth
session    required     /<%= archlib -%>/security/$ISA/pam_stack.so service=system-auth
session    optional     /<%= archlib -%>/security/$ISA/pam_xauth.so

